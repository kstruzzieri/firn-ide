# v0.14.0

| | |
| --- | --- |
| Status | Agreed 2026-09-25 |
| Freeze | Tue 2026-10-13 |
| Release | Tue 2026-10-20 |
| Milestone | [v0.14.0](https://github.com/kstruzzieri/firn-ide/milestone/2) |

The rules, exit criteria and release steps are in [README.md](README.md).

**Theme:** Golem moves to go-llm v0.3.0, and the defects users hit in the
editor, terminal, file tree and Golem get fixed. No new features.

## In

Sized in days (S 0.5, M 1.5, L 4, XL 8): 11.0 against a capacity of about 12.3
after the v0.13.0 release work (including #359 and #360) and the #353 plan.

| Issue | Size | Notes |
| --- | --- | --- |
| #306 go-llm v0.3.0 | L | First, for the longest soak. Folds in #286 items 1, 4 and 5. Decides `x-opencode-session` in the same pull request: v0.3.0 sends the conversation ID to every openai-compat endpoint, and that ID is stable and derived from the repository path, so strip it unless the destination is opencode, or send a blinded value. |
| #325 session store revision contract | S | Same pull request as #306, which makes the contract live. |
| #361 New chat resets the backend conversation | M | With #306 and #325, which rewrite the same store. |
| #256 stale file tree on open or reopen | M | |
| #275 terminal scrollback lost on collapse | M | Verify after a full reload; hot reload keeps stale observers. |
| #279 `Cmd+[` and `Cmd+]` shadowed by indent | S | Tests for both key sets. |
| #351 flaky Phase 2B drain test | S | Week 1, so it cannot fail Backend Tests, a required check, on pull requests before the freeze or on the release pull request into `main`. |
| #314 silent status messages | S | One persistent screen-reader status region. |
| #316 declared macOS floor | S | Declare the real floor in the README, both plists and the deployment target, and make the build target match it: macOS 12.3 with `build.target` `safari15.4`, or macOS 13 with Vite's default Safari 16. A build target alone adds no missing web API. |

If #359 or #360 misses v0.13.0, it joins In here by amendment, and the capacity
check is redone.

## Stretch

- #276: focus a new terminal session. After #275, which touches the same
  effects.
- #278: workspace selector focus. Close it if the packaged v3 build does not
  reproduce it.
- #283: contradictory run-profile status copy.

## Also scheduled

The #353 implementation plan and its review, written against the go-llm v0.3.0
API once #306 merges, so v0.15.0 can start on it directly.

## Out

Everything else. Notable:

- #355 goes to v0.15.0 with the rest of the configuration work.
- #264 (durable multi-conversation) is designed during v0.15.0 for v0.16.0.
- #281 (GTK4 and WebKitGTK 6.0 on Linux) is unscheduled. It can land on the
  current Wails pin, and it must land before Firn adopts Wails v3.1, which is
  unreleased.

## Order

Week 1: the v0.13.0 release, #351, then #306 with #325 and #361. Then #256,
#275 followed by #276, #279, #314 and #316.

## Exit criteria

The criteria in [README.md](README.md), plus:

- A live smoke of the Golem panel after the go-llm bump, docked and undocked,
  against an Ollama and an openai-compat destination.
- A test that pins which destinations receive `x-opencode-session`.

## Known issues

Recorded at the freeze.

## Amendments

None.
