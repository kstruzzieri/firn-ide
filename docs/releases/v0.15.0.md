# v0.15.0

| | |
| --- | --- |
| Status | Agreed 2026-09-25 |
| Freeze | Tue 2026-11-03 |
| Release | Tue 2026-11-10 |
| Milestone | [v0.15.0](https://github.com/kstruzzieri/firn-ide/milestone/3) |

The rules, exit criteria and release steps are in [README.md](README.md).

**Theme:** the Golem configuration workspace gets its role picker, and its
routing and consent disclosures say exactly what a change does.

## In

Sized in days (S 0.5, M 1.5, L 4, XL 8): 15.0 against a capacity of about 15.8
after the v0.14.0 release work and the #264 design.

| Issue | Size | Notes |
| --- | --- | --- |
| #335 explicit selector override in the projection | M | Week 1; #311 and #337 build on it. |
| #353 role picker | XL | Absorbs #334. User-named roles keep the trust-boundary rules: at most 256 bytes, no control or bidi characters, no duplicates within a batch. |
| #349 shared-model notice names the unrouted roles | S | One pull request with #307. |
| #307 the route editor's reach sentence | S | Derive it from the same reach data as the row markers. |
| #338 refusal names the use case, not a role | S | Rides with #353's planner changes. |
| #355 destination approval names every model and the path | L | Needs #306 from v0.14.0. The approval request gains a models list across the Go and TypeScript contract; the challenge digest does not change. |

## Stretch

- #311: the model picker and the Apply gate agree. After #335 and #353's
  planner changes.
- #342: two open editors on one selector.
- #350: the Apply-anyway prompt asks only when a change can narrow a use case.
- #337: the confirmation notice's wording. After #350.
- #323: show the missing floor capability on an Incompatible row.
- #318: table columns from one definition.
- #293: model card border contrast.
- #321: the profile-switch prompt. Needs a ruling on spec §4.6a first.

## Also scheduled

The #264 design (durable multi-conversation), for v0.16.0.

## Out

Everything else. Notable: #264; #263's add-use-case control and Slice D (the
inventory-backed picker), which need nothing new from go-llm and are not yet
scheduled; profile Export and Delete, which wait on go-llm#536 and go-llm#537;
#336, #317, #319 and #322.

## Exit criteria

The criteria in [README.md](README.md), plus a VoiceOver pass over the role
picker's listbox.

## Known issues

Recorded at the freeze.

## Amendments

None.
